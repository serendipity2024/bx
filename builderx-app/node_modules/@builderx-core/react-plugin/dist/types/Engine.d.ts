import { Config } from '../react-native/types';
export default interface Engine {
    getFileCode: (fileJSON: any, config: Config) => string;
    getAppFiles: (files: {
        artboards: string[];
        assets: string[];
    }) => {
        path: string;
        content: string;
    }[];
    getIndexWithFont: (assets: string[]) => {
        path: string;
        content: string;
    }[];
}
