const shell = require("./shellHelper");
const path = require("path");
const serverDirPath = path.resolve(__dirname, "../..", "server");

shell.exec(`yarn build`, { cwd: serverDirPath }, (err, stdout, stderr) => {
  if (err) {
    console.error(err, "err");
    return;
  }
  console.log(stdout);
});
