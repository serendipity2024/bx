const cypress = require("cypress");

cypress
  .run({
    browser: "chrome",
    spec: "./cypress/integration/actions.spec.js"
  })
  .then(results => {
    console.log(results);
  })
  .catch(err => {
    console.error(err);
  });
