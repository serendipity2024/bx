import IPC from "./IPC";

const codeEngineWorker: Worker = self as any;
const ipc = new IPC(codeEngineWorker);
codeEngineWorker.onmessage = event => {
  ipc.ipcOnMessage(event);
};

export default {};
