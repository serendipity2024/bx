import BaseCodeGen from "../BaseCodeGen";

export type ExtraParams = { parent: any, textLiteral?: any };

export interface Context {
  styles: {
    [key: string]: any
  };
  imports: {
    [key: string]: {
      named: Array<string | object>,
      default: string | false,
      cssFile?: string | undefined
    }
  };
  filePath: any;
  smartFlexTransformed: boolean;
  conditionals: any;
  isPlatformConditional: boolean;
  fileHasTabView: boolean;
  baseCodeGen: BaseCodeGen;
  switchDuplicate: {
    isDuplicate: boolean,
    lastNumber: number
  };
  mapDuplicate: {
    isDuplicate: boolean,
    lastNumber: number
  };
  buttonDuplicate: {
    isDuplicate: boolean,
    lastNumber: number,
    exists: boolean,
    codeGenerated: boolean
  };
}

export type Config = {
  isPreviewFile: boolean
};
