import { DomainLayer, DomainFile } from "domain-x";
import { Layer } from "@builderx-core/designer";
import { StageArtboard } from "session";

export const globalRefMap = new Map<
  DomainLayer | DomainFile | StageArtboard,
  Array<Layer | StageArtboard>
>();

if (process.env.NODE_ENV !== "production") {
  window["globalRefMap"] = globalRefMap;
}
