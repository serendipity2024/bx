import { hasSamePositionTypeInCoordinate } from "../utils";

test("hasSamePositionTypeInCoordinate in utils works", () => {
  expect(
    hasSamePositionTypeInCoordinate(
      "x",
      { left: "45%", right: "78%" },
      { left: "45%", right: "78%" }
    )
  ).toBe(true);

  // expect(
  //   hasSamePositionTypeInCoordinate(
  //     "y",
  //     { top: 45, height: 78, bottom: 98 },
  //     { top: 45, height: 78 }
  //   )
  // ).toBe(false);
});
