import * as React from "react";

import { Component } from "react";
import { WindowWrapper, Spinner } from "components-x";

export default class Init extends Component<any, any> {
  render() {
    return (
      <WindowWrapper>
        {!this.props.blank && <Spinner uiSize="l" />}
      </WindowWrapper>
    );
  }
}
