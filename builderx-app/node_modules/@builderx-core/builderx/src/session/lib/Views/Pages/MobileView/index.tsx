import * as React from "react";

import { Component } from "react";
import { WindowWrapper, Spinner, COLORS, Text, WrapperDiv } from "components-x";

export default class MobileView extends Component<any, any> {
  render() {
    return (
      <div
        style={{
          flex: 1,
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          height: "100%",
          width: "100%",
          overflow: "hidden",
          flexDirection: "column"
        }}
      >
        <div style={{ marginBottom: 5 }}>
          <Text uiSize={"xxl"}>
            BuilderX is only supported on Google Chrome browser for Desktop.
          </Text>
        </div>
        <div>
          <Text uiSize={"xxl"}>
            This browser is not supported at the moment.
          </Text>
        </div>
      </div>
    );
  }
}
