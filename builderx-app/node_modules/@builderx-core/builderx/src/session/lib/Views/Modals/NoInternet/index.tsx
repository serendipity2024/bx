import * as React from "react";
import {
  Window,
  WindowBody,
  Text,
  Pane,
  FormCol,
  FormRow,
  FormGroup,
  ModalContainer,
  ModalBackdrop,
  ModalContent,
  Title
} from "components-x";
import { Session } from "../../../models/Session";

export default class NoInternet extends React.Component<
  { session: Session },
  any
> {
  constructor(props: any) {
    super(props);
  }

  render() {
    return (
      <ModalContainer>
        <ModalBackdrop uiBackground={800} />
        <ModalContent uiSize="m">
          <Window uiSize="m">
            <WindowBody>
              <Pane
                uiBackground={800}
                padder={"xs"}
                style={{
                  display: "block",
                  overflow: "visible"
                }}
                className="py-0"
              >
                <FormGroup className="py-4 px-3">
                  <FormRow noPadding alignTop className="px-4 pt-4 pb-2">
                    <FormCol uiSize="xs" first alignTop>
                      <Title uiSize="m" uiColor={300}>
                        No Internet Connection!
                      </Title>
                    </FormCol>
                  </FormRow>
                  <FormRow className="px-4 pb-4 pt-2">
                    <FormCol first>
                      <Text uiSize={"m"} uiColor={400}>
                        Your computer seems to be offline. Please connect to
                        internet.
                      </Text>
                    </FormCol>
                  </FormRow>
                </FormGroup>
              </Pane>
            </WindowBody>
          </Window>
        </ModalContent>
      </ModalContainer>
    );
  }
}
