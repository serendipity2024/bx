import * as React from "react";
import { File as DomainFile } from "domain-x";
import { Grid, Col } from "components-x";
import StageToolbar from "../../Toolbars/StageToolbar";
import { Session } from "../../../../models/Session";

export default class StageToolbarPanel extends React.PureComponent<
  { session: Session },
  any
> {
  domainFiles: Map<string, DomainFile>;
  onUpdate = () => {
    this.forceUpdate();
  };
  componentDidMount() {
    // this.props.session.on(
    //   ["isNewProject", "isSaving", "hasWarning", "astNetworkRequestCounter", "networkRequestCounter"],
    //   this.onUpdate
    // );
    // this.props.session.project.on("basePath", this.onUpdate);
    // this.props.session.domain.files.forEach((file: DomainFile) => {
    //   file.on("isDirty", this.onUpdate);
    // });
    // this.domainFiles = this.props.session.domain.files;
  }
  componentDidUpdate() {
    // this.domainFiles.forEach((file: DomainFile) => {
    //   file.off("isDirty", this.onUpdate);
    // });
    // this.props.session.domain.files.forEach((file: DomainFile) => {
    //   file.on("isDirty", this.onUpdate);
    // });
  }
  componentWillUnmount() {
    // this.props.session.project.off("basePath", this.onUpdate);
    // this.props.session.off(
    //   ["isNewProject", "isSaving", "hasWarning", "astNetworkRequestCounter", "networkRequestCounter"],
    //   this.onUpdate
    // );
    // this.props.session.domain.files.forEach((file: DomainFile) => {
    //   file.off("isDirty", this.onUpdate);
    // });
  }
  render() {
    return (
      <Grid>
        <Col>
          <StageToolbar session={this.props.session} />
        </Col>
      </Grid>
    );
  }
}
