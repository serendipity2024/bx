import { Model } from "model";

export default class Viewport extends Model {
  width: number;
  height: number;
  setViewportSize(size: { width: number; height: number }) {
    this.width = size.width;
    this.height = size.height;
  }

  getFields() {
    return {};
  }
}
