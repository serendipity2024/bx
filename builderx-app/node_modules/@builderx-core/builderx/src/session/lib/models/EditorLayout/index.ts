import { action, Model } from "model";
export class EditorLayout extends Model {
  current: "layers" | "components" | undefined;

  constructor() {
    super();
    //
  }

  setCurrent(current: "layers" | "components" | undefined) {
    this.current = current;
  }
}
