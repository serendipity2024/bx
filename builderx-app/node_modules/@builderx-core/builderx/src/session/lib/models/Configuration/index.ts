import { get, merge } from "lodash";
import { Model } from "model";
const DefaultSettings = require("./settings.json");

export default class Configuration extends Model {
  configurations: any;
  defaultConfigurations: any;
  userConfigurations: any;
  workspaceConfigurations: any;
  userPath: string;
  settingPath: string;

  constructor() {
    super();
    //
    this.defaultConfigurations = DefaultSettings;
    this.configurations = DefaultSettings;
    // this.userPath = userDataPath + "/User";
    // this.settingPath = this.userPath + "/settings.json";
    // //console.log("path======== this.userPath", this.userPath);
    // //console.log("path======== this.settingPath", this.settingPath);
  }

  getFields() {
    return {};
  }

  get = (key: string): any => {
    return get(this.configurations, key);
  };
  getAll = () => {
    return this.configurations;
  };
  set = (key: string, value: any) => {
    // this.userConfigurations[key] = value;
    this.configurations[key] = value;
    // this.saveUserConfiguration();
  };
}
