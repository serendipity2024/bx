import { Layer, Event } from "@builderx-core/designer";

export default class DesignerKeyboardEvent extends Event {
  originalEvent: KeyboardEvent;
  name: string;
  target: Layer;
  constructor(name: string, target: Layer, originalEvent: any) {
    super(name, target);
    this.originalEvent = originalEvent;
  }
}
