import { ICommand } from "command-manager";
import { Session } from "../../models/Session";

export default class AddSnapToGutterLine implements ICommand {
  displayName = "AddSnapToGutterLine";
  session: Session;
  data: any;
  constructor(session: Session, data: any) {
    this.session = session;
    this.data = data;
  }
  execute() {
    // const currentStage = this.session.currentStage;
    // var line = SnapLineModel.fromJS(this.data);
    // currentStage.tools.snapToGutterLines.push(line);
    // currentStage.tools.emit("snapToGutterLines");
    // currentStage.tools.snapToGutterLines.forEach(snapLine => {
    //   snapLine.emit("length");
    // });
    // line.emit("length");
  }
  undo() {
    // nope
  }
  redo() {
    // nope
  }
}
