import { ICommand } from "command-manager";
import { Session } from "../../models/Session";

export default class SetSelectedRuler implements ICommand {
  displayName = "SetSelectedRuler";
  session: Session;
  constructor(session: Session) {
    this.session = session;
    // this.ruler = ruler;
  }
  execute() {
    this.session.drivers.analyticsService.addActivity(
      {
        name: `set.selected.ruler`,
        path: "",
        category: "app/command"
      },
      true
    );
    // this.session.selectedRuler = this.ruler;
    // this.session.emit("selectedRuler");
  }
  undo() {
    // nope
  }
  redo() {
    // nope
  }
}
