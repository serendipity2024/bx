import { ICommand } from "command-manager";
import { Session } from "../../models/Session";

export default class ClearSnapToGutterLines implements ICommand {
  displayName = "ClearSnapToGutterLines";
  session: Session;
  constructor(session: Session) {
    this.session = session;
  }
  execute() {
    // const currentStage = this.session.currentStage;
    // const prevLen = currentStage.tools.snapToGutterLines.length;
    // currentStage.tools.snapToGutterLines.length = 0;
    // if (prevLen > 0) {
    //   currentStage.tools.emit("snapToGutterLines");
    // }
    // currentStage.tools.snapToGutterLines.forEach(snapLine => {
    //   console.log(snapLine, "line here");
    //   snapLine.emit("length");
    // });
  }
  undo() {
    // nope
  }
  redo() {
    // nope
  }
}
