import { ICommand } from "command-manager";
import { Session } from "../../models/Session";

export default class RemoveVerticalSnapBar implements ICommand {
  displayName = "RemoveVerticalSnapBar";
  session: Session;
  constructor(session: Session) {
    this.session = session;
  }
  execute() {
    // const currentStage = this.session.currentStage;
    // const prevLen = currentStage.tools.snapBar.vertical.length;
    // currentStage.tools.snapBar.vertical.length = 0;
    // if (prevLen) {
    //   currentStage.tools.emit("snapBar");
    //   currentStage.tools.snapBar.vertical.emit("length");
    // }
  }
  undo() {
    // nope
  }
  redo() {
    // nope
  }
}
