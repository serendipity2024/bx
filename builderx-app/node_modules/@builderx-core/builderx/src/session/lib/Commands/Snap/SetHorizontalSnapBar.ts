import { ICommand } from "command-manager";
import { Session } from "../../models/Session";

export default class SetHorizontalSnapBar implements ICommand {
  displayName = "SetHorizontalSnapBar";
  session: Session;
  distance: number;
  constructor(session: Session, distance: number) {
    this.session = session;
    this.distance = distance;
  }
  execute() {
    // const currentStage = this.session.currentStage;
    // currentStage.tools.snapBar.horizontal.y = this.distance;
    // // currentStage.tools.snapBar.horizontal.length =
    // //   this.session.currentStage.renderCompiled.width * currentStage.tools.zoom;
    // currentStage.tools.emit("snapBar");
    // currentStage.tools.snapBar.horizontal.emit(["y", "length"]);
    // // currentStage.tools.snapBar.horizontal.emit("change");
  }
  undo() {
    // nope
  }
  redo() {
    // nope
  }
}
