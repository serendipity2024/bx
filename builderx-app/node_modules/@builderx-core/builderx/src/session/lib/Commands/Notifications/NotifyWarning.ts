import { ICommand } from "command-manager";
import { Session } from "../../models/Session";
import notifyWarningService from "../CommandServices/NotifyWarning";

export default class NotifyWarning implements ICommand {
  displayName = "NotifyWarning";
  session: Session;
  message: string;
  title: string;
  constructor(session: Session, message: string, title: string) {
    this.session = session;
    this.message = message;
    this.title = title;
  }
  execute() {
    notifyWarningService(this.session, this.title, this.message);
  }
  undo() {
    //
  }
  redo() {
    //
  }
}
