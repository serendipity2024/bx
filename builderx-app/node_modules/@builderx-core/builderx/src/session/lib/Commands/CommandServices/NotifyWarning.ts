import { Session } from "../../models/Session";
import createService from ".";

const NotifyWarning = createService(
  "NotifyWarning",
  (session: Session, title: string, message: string | string[]) => {
    session.notificationManager.notifyWarning(message, title);
  }
);
export default NotifyWarning;
