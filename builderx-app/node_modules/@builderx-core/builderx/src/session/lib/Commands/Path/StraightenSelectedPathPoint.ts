import { ICommand } from "command-manager";
import { Session } from "../../models/Session";
import { SetPathMutator } from "../../Mutators";

export default class StraightenSelectedPathPoint implements ICommand {
  displayName = "StraightenSelectedPathPoint";
  session: Session;
  constructor(session: Session) {
    this.session = session;
  }
  execute() {
    SetPathMutator(this.session, "straight");
  }
  undo() {
    //
  }
  redo() {
    //
  }
}
