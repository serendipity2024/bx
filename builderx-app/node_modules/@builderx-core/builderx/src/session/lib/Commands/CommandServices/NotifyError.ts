import { Session } from "../../models/Session";
import createService from ".";

const NotifyError = createService(
  "NotifyError",
  (session: Session, title: string, message: string | string[]) => {
    session.notificationManager.notifyError(message, title);
  }
);

export default NotifyError;
