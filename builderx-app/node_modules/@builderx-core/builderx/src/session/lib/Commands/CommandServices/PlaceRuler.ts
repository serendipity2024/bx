import { Session } from "../../models/Session";
import createService from ".";

const PlaceRuler = createService("PlaceRuler", (session: Session) => {
  const selectedFile = session.selectedFile;
  // if (selectedFile) {
  //   var selectedFileRulers = session.project.rulers.get(selectedFile.path);
  //   if (selectedFileRulers) {
  //     selectedFileRulers.map(ruler => {
  //       const fileDistance =
  //         ruler.direction === "horizontal"
  //           ? selectedFile.getBoundingClientRect().rootY
  //           : selectedFile.getBoundingClientRect().rootX;

  //       ruler.distance =
  //         (ruler.originalDistance + fileDistance) *
  //         session.currentStage.tools.zoom;
  //     });
  //   }
  // }
});

export default PlaceRuler;
