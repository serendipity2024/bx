import { ICommand } from "command-manager";
import { Session } from "../../models/Session";
import { SetPathMutator } from "../../Mutators";

export default class DisconnectSelectedPathPoint implements ICommand {
  displayName = "DisconnectSelectedPathPoint";
  session: Session;
  constructor(session: Session) {
    this.session = session;
  }
  execute() {
    SetPathMutator(this.session, "disconnect");
  }
  undo() {
    //
  }
  redo() {
    //
  }
}
