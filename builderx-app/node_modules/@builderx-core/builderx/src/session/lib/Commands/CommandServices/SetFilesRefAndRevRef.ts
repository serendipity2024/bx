import { File } from "domain-x";
import createService from ".";

export const SetFilesRefAndRevRef = createService(
  "SetFilesRefAndRevRef",
  (files: { domainFile: File; compiledFile: any; renderCompiledFile: any }) => {
    // if (!(files.domainFile.layer instanceof Layer)) {
    //   return;
    // }
    // add file reverse ref
  }
);
