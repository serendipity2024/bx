import { ICommand } from "command-manager";
import { Session } from "../../models/Session";

export default class SetVerticalSnapBar implements ICommand {
  displayName = "SetVerticalSnapBar";
  session: Session;
  distance: number;
  constructor(session: Session, distance: number) {
    this.session = session;
    this.distance = distance;
  }
  execute() {
    // const currentStage = this.session.currentStage;
    // currentStage.tools.snapBar.vertical.x = this.distance;
    // // currentStage.tools.snapBar.vertical.length =
    // //   this.session.currentStage.renderCompiled.height * currentStage.tools.zoom;
    // currentStage.tools.emit("snapBar");
    // currentStage.tools.snapBar.vertical.emit(["x", "length"]);
  }
  undo() {
    // nope
  }
  redo() {
    // nope
  }
}
