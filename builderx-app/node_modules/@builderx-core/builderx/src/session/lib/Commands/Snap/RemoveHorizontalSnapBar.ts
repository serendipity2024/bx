import { ICommand } from "command-manager";
import { Session } from "../../models/Session";

export default class RemoveHorizontalSnapBar implements ICommand {
  displayName = "RemoveHorizontalSnapBar";
  session: Session;
  constructor(session: Session) {
    this.session = session;
  }
  execute() {
    // const currentStage = this.session.currentStage;
    // const prevLen = currentStage.tools.snapBar.horizontal.length;
    // currentStage.tools.snapBar.horizontal.length = 0;
    // if (prevLen) {
    //   currentStage.tools.emit("snapBar");
    //   currentStage.tools.snapBar.horizontal.emit("length");
    // }
  }
  undo() {
    // nope
  }
  redo() {
    // nope
  }
}
