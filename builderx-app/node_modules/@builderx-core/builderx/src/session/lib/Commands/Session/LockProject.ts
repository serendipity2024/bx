import { ICommand } from "command-manager";
import { Session } from "../../models/Session";
import LockProjectMutator from "../../Mutators/LockProjectMutator";

export default class LockProject implements ICommand {
  displayName = "LockProject";
  session: Session;
  constructor(session: Session) {
    this.session = session;
  }

  execute() {
    LockProjectMutator(this.session);
  }
  undo() {
    // nope
  }
  redo() {
    // nope
  }
}
