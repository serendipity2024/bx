import { ICommand } from "command-manager";
import { Session } from "../../models/Session";
import SetZoom from "./SetZoom";
import CommandKeyMapperService from "../../services/CommandKeyMapperService";

export default class SetLayerEditedBy implements ICommand {
  displayName = "SetLayerEditedBy";
  session: Session;
  data: any;
  constructor(session: Session, data: any) {
    this.session = session;
    this.data = data;
  }

  execute() {
    this.session.project.setLastEditedBy(this.data);
  }
  undo() {
    // nope
  }
  redo() {
    // nope
  }
}
