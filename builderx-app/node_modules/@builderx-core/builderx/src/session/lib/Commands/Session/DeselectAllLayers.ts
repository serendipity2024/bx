import { ICommand } from "command-manager";
import { Session } from "../../models/Session";
import DeselectAllLayersService from "../CommandServices/DeselectAllLayers";

export default class DeselectAllLayers implements ICommand {
  displayName = "DeselectAllLayers";
  session: Session;
  constructor(session: Session) {
    this.session = session;
  }
  execute() {
    DeselectAllLayersService(this.session);
  }
  undo() {
    // nope
  }
  redo() {
    // nope
  }
}
