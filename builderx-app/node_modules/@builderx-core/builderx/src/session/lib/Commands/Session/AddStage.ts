import { ICommand } from "command-manager";
import { Session } from "../../models/Session";
import { AddStageMutator } from "../../Mutators";

export default class AddStage implements ICommand {
  displayName = "AddStage";
  private session: Session;
  private stageId: string;
  private layerPath: string;
  constructor(session: Session, stageId: string) {
    this.session = session;
    this.stageId = stageId;
  }
  execute() {
    AddStageMutator(this.session, this.stageId);
  }

  undo() {
    //
  }
  redo() {
    //
  }
}
