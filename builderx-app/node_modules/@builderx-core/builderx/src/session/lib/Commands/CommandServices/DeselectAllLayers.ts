import { Session } from "../../models/Session";
import DeselectAllLayersMutator from "../../Mutators/DeselectAllLayersMutator";
import createService from ".";
const DeselectAllLayers = createService(
  "DeselectAllLayers",
  (session: Session, generatePreview: boolean = false) => {
    DeselectAllLayersMutator(session, generatePreview);
  }
);

export default DeselectAllLayers;
