import { Session } from "../../models/Session";
import createService from ".";

const ClearRecordingConditions = createService(
  "ClearRecordingConditions",
  (session: Session, shouldEmitUpdates: boolean = true) => {
    session.currentStage.recordingConditions = {};
    if (shouldEmitUpdates) {
      if (session.selectedFile) {
        // session.selectedFile.emit("scopeVariables");
        // session.selectedFile.emit("conditionalExpList");
      }
      // TODO: remove this @ts-ignore
      // @ts-ignore
      // session.currentStage.emit("recordingConditions");
    }
  }
);
export default ClearRecordingConditions;
