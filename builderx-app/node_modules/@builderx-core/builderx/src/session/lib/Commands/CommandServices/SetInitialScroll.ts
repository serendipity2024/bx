import { Session } from "../../models/Session";
import createService from ".";

const SetInitialScroll = createService(
  "setInitialScroll",
  (
    session: Session,
    scroll: {
      x: number;
      y: number;
    }
  ) => {
    const currentStage = session.currentStage;

    // TODO: move to mutator
    // currentStage.canvas.executeFunction("setScroll", scroll);
  }
);

export default SetInitialScroll;
