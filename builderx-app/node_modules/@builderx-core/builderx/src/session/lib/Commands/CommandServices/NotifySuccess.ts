import { Session } from "../../models/Session";
import createService from ".";
const NotifySuccess = createService(
  "NotifySuccess",
  (session: Session, title: string, message: string | string[]) => {
    session.notificationManager.notifySuccess(message, title);
  }
);

export default NotifySuccess;
