export default function createService(name: string, callback: any) {
  return (...args: any) => {
    if (window["debug"] && window["debugLog"]) {
      window["debugLog"]("Service: " + name);
    }
    return callback(...args);
  };
}
