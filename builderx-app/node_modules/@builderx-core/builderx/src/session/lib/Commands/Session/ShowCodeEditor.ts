import { ICommand } from "command-manager";
import { Session } from "../../models/Session";
import SetFileCode from "./../CommandServices/SetFileCode";
import { SelectLayersMutator, ShowCodeEditorMutator } from "../../Mutators";
import { SessionEvent } from "event-manager";

export default class ShowCodeEditor implements ICommand {
  displayName = "ShowCodeEditor";
  session: Session;
  constructor(session: Session) {
    this.session = session;
  }
  execute() {
    ShowCodeEditorMutator(this.session);
    SetFileCode(this.session);
  }
  undo() {
    // nope
  }
  redo() {
    // nope
  }
}
