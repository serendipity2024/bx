import { Session } from "../../models/Session";
import createService from ".";

const NotifyInfo = createService(
  "NotifyInfo",
  (session: Session, title: string, message: string | string[]) => {
    session.notificationManager.notifyInfo(message, title);
  }
);

export default NotifyInfo;
