import { ICommand } from "command-manager";
import { Session } from "../../models/Session";
// import { PlaceArtboardsToCenter } from "..";
import { DisplayMode } from "common-interfaces";
import changeDisplayModeService from "../CommandServices/ChangeDisplayMode";
// import previewFile from "../CommandServices/PreviewFile";
// // import PreviewFile from "./PreviewFile";

export default class ChangeDisplayMode implements ICommand {
  displayName = "ChangeDisplayMode";
  session: Session;
  mode: DisplayMode;
  constructor(session: Session, mode: DisplayMode) {
    this.session = session;
    this.mode = mode;
  }
  execute() {
    // changeDisplayModeService(this.session, this.mode);
    // this.session.drivers.analyticsService.addActivity(
    //   {
    //     name: `change.display.mode`,
    //     path: "",
    //     category: "app/command"
    //   },
    //   true
    // );
  }
  undo() {
    // nope
  }
  redo() {
    // nope
  }
}
