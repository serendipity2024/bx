import { ICommand, CommandManager } from "command-manager";
import { Session } from "../../models/Session";
import { DraggedSharedComponent } from "common-interfaces";
import { get } from "lodash";

export default class SetDraggedSharedComponent implements ICommand {
  displayName = "SetDraggedSharedComponent";
  private session: Session;
  private draggedSharedComponent: DraggedSharedComponent | undefined;
  constructor(
    session: Session,
    draggedSharedComponent: DraggedSharedComponent | undefined
  ) {
    this.session = session;
    this.draggedSharedComponent = draggedSharedComponent;
  }
  execute() {
    this.session.selectedSharedComponent = this.draggedSharedComponent;
    // this.session.currentStage.tools.sharedComponentsGhost.height = get(
    //   this.draggedSharedComponent,
    //   "height",
    //   0
    // );
    // this.session.currentStage.tools.sharedComponentsGhost.width = get(
    //   this.draggedSharedComponent,
    //   "width",
    //   0
    // );
  }
  undo() {
    //
  }
  redo() {
    //
  }
}
