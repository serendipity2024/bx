import { Session } from "session";
import { SetAllScopeVariablesMutator } from "../../Mutators";
import createService from ".";

export const SetAllScopeVariablesService = createService(
  "SetAllScopeVariablesService",
  (session: Session, layerPath: string, variableObj: any) => {
    SetAllScopeVariablesMutator(session, layerPath, variableObj);
  }
);
