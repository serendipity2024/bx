import { IMutator } from "common-interfaces";
import { Session } from "../models/Session";
import { forEach } from "lodash";
import { Layer } from "@builderx-core/designer";
import { SessionEvent } from "event-manager";

export default function SetGridVisibilityMutator(
  session: Session,
  value: boolean
) {
  session.emitPatch({
    op: "replace",
    path: `/drivers/configuration/configurations/editor.show.grid`,
    value: value
  });
  // session.drivers.configuration.set("editor.show.grid", value);

  // session.drivers.configuration.emit("editor.show.grid");

  // const event = new SessionEvent("setGridVisibility", {
  //   value
  // });
  // session.eventManager.dispatchEvent("setGridVisibility", event);
}
