import { IMutator } from "common-interfaces";
import { Session } from "../models/Session";
import { SessionEvent } from "event-manager";

export default function ToggleSnap(session: Session, value: boolean) {
  // session.drivers.configuration.set("editor.snap", value);
  // session.drivers.configuration.emit("editor.snap");
  session.emitPatch({
    op: "replace",
    path: `/drivers/configuration/configurations/editor.snap`,
    value: value
  });

  // const event = new SessionEvent("toggleSnap", {
  //   value
  // });
  // session.eventManager.dispatchEvent("toggleSnap", event);
}
