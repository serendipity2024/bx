import { Session } from "../models/Session";
import { DisplayMode } from "common-interfaces";
import { SessionEvent } from "event-manager";

export default function ChangeDisplayModeMutator(
  session: Session,
  mode: DisplayMode
) {
  session.emitPatch({
    op: "replace",
    path: `/displayMode`,
    value: mode
  });
}
