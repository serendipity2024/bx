import { IMutator } from "common-interfaces";
import { Session } from "../models/Session";
import { SessionEvent } from "event-manager";

export default function SetRulerVisibilityMutator(
  session: Session,
  value: boolean
) {
  // session.drivers.configuration.set("editor.show.ruler", value);
  session.emitPatch({
    op: "replace",
    path: `/drivers/configuration/configurations/editor.show.ruler`,
    value: value
  });

  // session.drivers.configuration.emit("editor.show.ruler");

  // const event = new SessionEvent("setRulerVisibility", {
  //   value
  // });
  // session.eventManager.dispatchEvent("setRulerVisibility", event);
}
