import { DomainLayer, DomainFile } from "domain-x";
import { Session } from "session";
import { SessionEvent } from "event-manager";

export default function changeDisplayModePatch(session: Session, mode: string) {
  const event = new SessionEvent("changeDisplayMode", {
    mode
  });

  session.eventManager.dispatchEvent("changeDisplayMode", event);
}
