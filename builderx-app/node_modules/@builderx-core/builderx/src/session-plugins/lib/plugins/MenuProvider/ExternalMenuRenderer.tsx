import * as React from "react";
import { MenuItem } from "./types";
import {
  Icon
  // OptionNew as Option,
  // OptionMenuNew as OptionMenu,
  // MenuDropdownNew as MenuDropdown,
  // OptionMenuLeftNew as OptionMenuLeft,
  // OptionMenuTitleNew as OptionMenuTitle,
  // OptionMenuRightNew as OptionMenuRight
} from "components-x";
import { Session } from "session";

export function renderExternalMenu(
  session: Session,
  filteredMenuItems: Array<() => MenuItem | undefined>
) {
  return filteredMenuItems.map((itemCallback: () => MenuItem | undefined) => {
    let item = itemCallback();
    if (!item) {
      return null;
    }
    return (
      <div key={item.caption + (item.placement || "")} />
      // <Option
      //   key={item.caption + (item.placement || "")}
      //   id={item.caption}
      //   onClick={
      //     item && item.callback
      //       ? (e: any) => (item && item.callback ? item.callback(e) : {})
      //       : undefined
      //   }
      // >
      //   <OptionMenu>
      //     <OptionMenuLeft>
      //       {item.leftIcon ? <Icon name={item.leftIcon} size={12} /> : null}
      //     </OptionMenuLeft>
      //     <OptionMenuTitle>{item.caption}</OptionMenuTitle>
      //     <OptionMenuRight>
      //       {item.rightIcon ? (
      //         <Icon name={item.rightIcon} size={item.iconSize || 12} />
      //       ) : item.shortcut ? (
      //         item.shortcut
      //       ) : null}
      //     </OptionMenuRight>
      //   </OptionMenu>
      //   {item.children && item.children.length ? (
      //     <MenuDropdown child>
      //       {renderExternalMenu(session, item.children)}
      //     </MenuDropdown>
      //   ) : null}
      // </Option>
    );
  });
}
