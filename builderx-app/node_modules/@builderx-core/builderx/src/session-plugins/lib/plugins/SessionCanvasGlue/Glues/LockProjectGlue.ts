import { SessionEvent } from "event-manager";
import { Session } from "session";

function applySideEffects(session: Session) {
  session.currentStage.canvas.executeFunction("hidePlusButton");
}

export default function LockProjectGlue(session: Session, e: SessionEvent) {
  applySideEffects(session);
}
