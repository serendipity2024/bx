import DomainLayer from "./Base/Domain/Layer";

export type Dimension = {
  x: number;
  y: number;
  width: number;
  height: number;
};
export type Position = {
  x: number;
  y: number;
};
export type Size = { width: any; height: any };

type AllLayers = DomainLayer;
export type ChildrenTypes<T> = T extends DomainLayer ? AllLayers : any;

// type AllCompiledLayers = ViewCompiledLayer;
// export type CompiledChildrenTypes<T> = T extends ViewCompiledLayer
//   ? AllCompiledLayers // children types for ViewLayer //   : T extends TextLayer
//   : //     ? TextLayer // children types for TextLayer
//     AllCompiledLayers;
