import { ComponentDefinition } from "common-interfaces";

export default class SharedComponentLibrary {
  name: string;
  symbols: Map<string, any>;
  templates: { [key: string]: Array<ComponentDefinition> };
}
