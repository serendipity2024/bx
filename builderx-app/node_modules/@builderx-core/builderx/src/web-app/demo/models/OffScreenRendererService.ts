// import { Model } from "model";
// import { Session } from "session";

// import { IOffScreenRendererService } from "common-interfaces";

// const electron = window["require"]("electron");
// const ipcRenderer = electron.ipcRenderer;
// const currentWindow = electron.remote.getCurrentWindow();

// export default class OffScreenRendererService extends Model
//   implements IOffScreenRendererService {
//   session: Session;
//   constructor(session: Session) {
//     super();
//     this.session = session;
//   }
//   showOffscreenLoader(heading: string, subHeading: string) {
//     const currentRoute = this.session.pageRouter.currentRoute;
//     if (currentRoute !== "init") {
//       ipcRenderer.send("setOffScreenModalContent", [
//         heading,
//         subHeading,
//         currentWindow.id
//       ]);
//       ipcRenderer.send("showOffScreenModal", currentWindow.id);
//     }
//   }
//   showProgressOffscreenLoader() {
//     ipcRenderer.send("showOffScreenModal", currentWindow.id);
//   }
//   setContentProgressOffscreenLoader(
//     heading: string,
//     subHeading: string,
//     currentProgress: number
//   ) {
//     ipcRenderer.send("setOffScreenModalProgressContent", [
//       currentWindow.id,
//       currentProgress,
//       heading,
//       subHeading
//     ]);
//   }
//   hideOffscreenLoader() {
//     ipcRenderer.send("hideOffScreenModal", currentWindow.id);
//   }
// }
