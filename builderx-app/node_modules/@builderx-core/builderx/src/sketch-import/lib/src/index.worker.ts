import IPC from "./IPC";

const sketchImportWorker: Worker = self as any;
const ipc = new IPC(sketchImportWorker);
sketchImportWorker.onmessage = event => {
  ipc.ipcOnMessage(event);
};

export default {};
