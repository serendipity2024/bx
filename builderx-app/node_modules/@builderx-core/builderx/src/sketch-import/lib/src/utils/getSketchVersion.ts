export default function getSketchVersion(metaData: any): number {
  // gets the sketch version from meta file, if not present, gives 43
  return Number(metaData.appVersion) ? parseFloat(metaData.appVersion) : 43;
}
