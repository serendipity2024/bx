export type ExtractedData = {
  meta: any;
  pages: Array<any>;
  images: any; // Map<string, ImageBitmap>;
};

export type ConvertedData = { artboards: Array<any>; symbols: Array<any> };

export type LayerData = { artboard: any; symbols: Array<any> };
