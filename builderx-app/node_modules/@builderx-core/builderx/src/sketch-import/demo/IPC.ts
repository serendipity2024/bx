// import * as uuid from "uuid/v4";

export default class IPC {
  messageIPCQueue: any = {};
  sketchImport: any;
  worker: Worker;
  ipcQueue: any;
  id: string;

  constructor() {
    // this.sketchImport = new SketchImport(this);
  }

  executeCommand(command: string, args: Array<any>) {
    //
  }
  async ipcOnMessage(event: any) {
    //
  }
}
