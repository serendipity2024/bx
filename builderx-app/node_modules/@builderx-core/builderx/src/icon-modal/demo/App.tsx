import * as React from "react";
import { IconModal } from "../lib";
import { WrapperDiv, Button } from "components-x";

class App extends React.Component<any, any> {
  constructor(props: any) {
    super(props);
  }
  public render() {
    return (
      <>
        <IconModal
          showIconModal={true}
          iconFamily="All"
          selectedIcon={(iconName: string, iconFamily: string) => {
            //
          }}
        />
      </>
    );
  }
}

export default App;
