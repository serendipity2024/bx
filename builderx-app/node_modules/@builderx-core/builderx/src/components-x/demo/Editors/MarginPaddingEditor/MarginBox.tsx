import * as React from "react";
import {
  FormGroup,
  FormCol,
  WrapperDiv,
  MarginLayout,
  MarginLock,
  Icon,
  FormRow,
  Slider,
  InputGroupInput,
  MarginTopSelector,
  MarginLeftSelector,
  MarginRightSelector,
  MarginBottomSelector,
  Text
} from "components-x";

export default class extends React.Component<any, any> {
  render() {
    return (
      <FormGroup noPadding style={{ display: "table-caption" }}>
        <FormCol first>
          <Text uiSize="m">{"Margin Editor Title"}</Text>
        </FormCol>
        <FormCol first uiSize={4} style={{ paddingTop: 12 }}>
          <WrapperDiv uiBackground={700} style={{ padding: 8 }}>
            <MarginLayout>
              <MarginTopSelector selected={true} />
              <MarginLeftSelector selected={false} />
              <MarginRightSelector selected={false} />
              <MarginBottomSelector selected={false} />
              <MarginLock>
                <Icon name={"unlock"} size={16} />
              </MarginLock>
            </MarginLayout>
          </WrapperDiv>
        </FormCol>
        <FormCol first>
          <FormRow style={{ paddingLeft: 0, paddingRight: 0 }}>
            <FormCol first uiSize={3}>
              <Slider min={0} max={100} value={0} />
            </FormCol>
            <FormCol uiSize={2}>
              <InputGroupInput
                undo={this.props.session.commandManager.undo}
                redo={this.props.session.commandManager.redo}
                type="number"
                value={0}
              />
            </FormCol>
          </FormRow>
        </FormCol>
      </FormGroup>
    );
  }
}
