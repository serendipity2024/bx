import * as React from "react";
import {
  FormRow,
  FormCol,
  InputGroup,
  InputGroupLeft,
  InputGroupRight,
  InputGroupInput,
  Text
} from "components-x";

export default class PositionEditor extends React.PureComponent<any, any> {
  render() {
    return (
      <FormRow>
        <FormCol first={true}>
          <InputGroup>
            <InputGroupLeft>
              <Text uiSize="xs">X</Text>
            </InputGroupLeft>
            <InputGroupRight>
              <InputGroupInput
                undo={this.props.session.commandManager.undo}
                redo={this.props.session.commandManager.redo}
                type="number"
                value={0}
              />
            </InputGroupRight>
          </InputGroup>
        </FormCol>
        <FormCol>
          <InputGroup>
            <InputGroupLeft>
              <Text uiSize="xs">Y</Text>
            </InputGroupLeft>
            <InputGroupRight>
              <InputGroupInput
                undo={this.props.session.commandManager.undo}
                redo={this.props.session.commandManager.redo}
                type="number"
                value={0}
              />
            </InputGroupRight>
          </InputGroup>
        </FormCol>
      </FormRow>
    );
  }
}
