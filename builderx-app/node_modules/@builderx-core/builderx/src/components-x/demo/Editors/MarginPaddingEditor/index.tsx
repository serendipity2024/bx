import * as React from "react";

import MarginBoxContainer from "./MarginBoxContainer";
import PaddingBoxContainer from "./PaddingBoxContainer";
import ChildMarginBoxContainer from "./ChildMarginBoxContainer";
import { FormGroup, FormRow, FormCol } from "components-x";

export default class MarginPaddingEditor extends React.PureComponent<any, any> {
  render() {
    return (
      <FormGroup style={{ paddingTop: 0, paddingBottom: 0 }}>
        <FormRow wrapContent>
          <FormCol first>
            <PaddingBoxContainer title={"Padding"} />
          </FormCol>
          <FormCol>
            <MarginBoxContainer title={"Margin"} />
          </FormCol>
          <FormCol>
            <ChildMarginBoxContainer title={"Children Margin"} />
          </FormCol>
        </FormRow>
      </FormGroup>
    );
  }
}
