import * as React from "react";

import ResizingTypeBox from "./ResizingTypeBox";
import { FormGroup, FormRow, FormCol, FixedButton, Text } from "components-x";
export default class ResizingTypeEditor extends React.PureComponent<any, any> {
  render() {
    return (
      <FormGroup>
        <FormRow>
          <FormCol />
          <FormCol first uiSize={4}>
            <ResizingTypeBox />
          </FormCol>
          <FormCol uiSize={8}>
            <FormRow>
              <FormCol first>
                <FixedButton position="right" selected={false} />
              </FormCol>
              <FormCol uiSize={3}>
                <Text uiSize="s">Fix Width</Text>
              </FormCol>
            </FormRow>
            <FormRow>
              <FormCol first>
                <FixedButton selected={false} />
              </FormCol>
              <FormCol uiSize={3}>
                <Text uiSize="s">Fix Height</Text>
              </FormCol>
            </FormRow>
          </FormCol>
        </FormRow>
      </FormGroup>
    );
  }
}
