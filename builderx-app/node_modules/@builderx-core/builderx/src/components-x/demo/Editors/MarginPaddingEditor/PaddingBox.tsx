import * as React from "react";
import {
  FormGroup,
  FormCol,
  WrapperDiv,
  PaddingLayout,
  PaddingLock,
  Icon,
  FormRow,
  Slider,
  InputGroupInput,
  Text,
  PaddingTopSelector,
  PaddingLeftSelector,
  PaddingRightSelector,
  PaddingBottomSelector
} from "components-x";

export default class extends React.Component<any, any> {
  render() {
    return (
      <FormGroup noPadding style={{ display: "table-caption" }}>
        <FormCol first>
          <Text uiSize="m">Padding Editor Title</Text>
        </FormCol>
        <FormCol first uiSize={4} style={{ paddingTop: 12 }}>
          <WrapperDiv uiBackground={700} style={{ padding: 8 }}>
            <PaddingLayout>
              <PaddingTopSelector selected={true} />
              <PaddingLeftSelector selected={true} />
              <PaddingRightSelector selected={true} />
              <PaddingBottomSelector selected={true} />
              <PaddingLock>
                <Icon name={"unlock"} size={16} />
              </PaddingLock>
            </PaddingLayout>
          </WrapperDiv>
        </FormCol>
        <FormCol first>
          <FormRow style={{ paddingLeft: 0, paddingRight: 0 }}>
            <FormCol first uiSize={3}>
              <Slider min={0} max={100} value={0} />
            </FormCol>
            <FormCol uiSize={2}>
              <InputGroupInput
                undo={this.props.session.commandManager.undo}
                redo={this.props.session.commandManager.redo}
                type="number"
                value={0}
              />
            </FormCol>
          </FormRow>
        </FormCol>
      </FormGroup>
    );
  }
}
