import * as React from "react";
import {
  FormRow,
  FormCol,
  InputGroup,
  InputGroupLeft,
  Icon,
  InputGroupRight,
  InputGroupInput,
  ButtonGroupLeft,
  ButtonGroupRight,
  ButtonGroup,
  Button
} from "components-x";

export default class TransformEditor extends React.Component<any, any> {
  render() {
    return (
      <FormRow>
        <FormCol first={true} uiSize={1}>
          <InputGroup>
            <InputGroupLeft>
              <Icon name="icon-transform" size={11} />
            </InputGroupLeft>
            <InputGroupRight>
              <InputGroupInput
                undo={this.props.session.commandManager.undo}
                redo={this.props.session.commandManager.redo}
                type="number"
                value={0}
              />
            </InputGroupRight>
          </InputGroup>
        </FormCol>
        <FormCol flex={true} uiSize={1}>
          <ButtonGroupLeft width={30}>
            <Icon name="mirror" size={11} />
          </ButtonGroupLeft>
          <ButtonGroupRight>
            <ButtonGroup uiSize="xs">
              <Button
                disabled={true}
                borderRight
                active
                uiBackground={600}
                flexible={true}
                icon={<Icon name="flip1" size={13} />}
              />
              <Button
                disabled={true}
                uiBackground={600}
                flexible={true}
                icon={<Icon name="flip2" size={13} />}
              />
            </ButtonGroup>
          </ButtonGroupRight>
        </FormCol>
      </FormRow>
    );
  }
}
