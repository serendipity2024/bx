import * as React from "react";
import { Component } from "react";
import RowElement from "./RowElement";
import ColumnElement from "./ColumnElement";
import {
  FormRow,
  FormCol,
  Button,
  Icon,
  ButtonGroup,
  Checkbox,
  CheckboxTick,
  CheckboxLabel,
  PropertyHeader,
  Text
} from "components-x";

class Parent extends Component<any, any> {
  getRowOrColumnElements = () => {
    return (
      <>
        <RowElement layer={this.props.layer} onEvent={this.props.onEvent} />
        <ColumnElement layer={this.props.layer} onEvent={this.props.onEvent} />
      </>
    );
  };
  getFlexRootElements = () => {
    return (
      <>
        <FormRow>
          <FormCol first>
            <Button
              active={true}
              borderLeft
              borderRight
              flexible
              caption={"Horizontal"}
              uiBackground={700}
              icon={<Icon marginRight name="icon-horizontal" size={13} />}
            />
          </FormCol>
          <FormCol>
            <Button
              active={false}
              borderRight
              flexible
              caption={"Vertical"}
              uiBackground={700}
              icon={<Icon marginRight name="icon-vertical" size={13} />}
            />
          </FormCol>
        </FormRow>
        {this.getRowOrColumnElements()}
        <FormRow>
          <FormCol first uiSize={1}>
            <ButtonGroup uiSize="xs">
              <Button flexible borderRight uiBackground={700} active={false}>
                {/* <Tooltip>
                  <Icon name="no-wrap" size={14} />
                  <TooltipText>No Wrap</TooltipText>
                </Tooltip> */}
              </Button>

              <Button flexible active={false} uiBackground={700}>
                {/* <Tooltip>
                  <Icon name="wrap" size={14} />
                  <TooltipText>Wrap</TooltipText>
                </Tooltip> */}
              </Button>
            </ButtonGroup>
          </FormCol>
          <FormCol uiSize={2}>
            <Checkbox>
              <CheckboxTick
                checked={true}
                onChange={() => {
                  //
                }}
              />
              <CheckboxLabel>Reverse Layout</CheckboxLabel>
            </Checkbox>
          </FormCol>
        </FormRow>
      </>
    );
  };
  showCollapseEditor = () => {
    return (
      <>
        <FormRow>
          <FormCol first uiSize={4}>
            <FormRow noPadding noTopPadding>
              <Button
                borderTopRadius
                active={true}
                uiBackground={700}
                borderRight={true}
                flexible={true}
                icon={<Icon name="icon-absolute" size={30} />}
                style={{ paddingTop: 13 }}
              />
            </FormRow>
            <FormRow noPadding noTopPadding>
              <Button
                borderBottomRadius
                active={true}
                uiBackground={700}
                borderRight={true}
                flexible={true}
                caption="Absolute"
                style={{ paddingBottom: 10 }}
              />
            </FormRow>
          </FormCol>
          <FormCol uiSize={4}>
            <FormRow noPadding noTopPadding>
              <Button
                active={false}
                borderTopRadius
                uiBackground={700}
                flexible={true}
                icon={<Icon name="icon-flexlayout" size={30} />}
                style={{ paddingTop: 13 }}
              />
            </FormRow>
            <FormRow noPadding noTopPadding>
              <Button
                borderBottomRadius
                active={false}
                uiBackground={700}
                flexible={true}
                caption="Flex"
                style={{ paddingBottom: 10 }}
              />
            </FormRow>
          </FormCol>
        </FormRow>
        {this.getFlexRootElements()}
      </>
    );
  };

  render() {
    return (
      <React.Fragment>
        <PropertyHeader uiSize="xs">
          <Text uiSize="m">Flex Layout</Text>
        </PropertyHeader>
        {this.showCollapseEditor()}
      </React.Fragment>
    );
  }
}

export default Parent;
