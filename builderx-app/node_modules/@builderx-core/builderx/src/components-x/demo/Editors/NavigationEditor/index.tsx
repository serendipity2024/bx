import * as React from "react";
import { FormRow, FormCol, Text } from "components-x";

export default class NavigationEditor extends React.Component<any, any> {
  render() {
    return (
      <React.Fragment>
        <FormRow style={{ paddingBottom: 10 }}>
          <FormCol first uiSize={5}>
            <Text>"Navigation"</Text>
          </FormCol>
          <FormCol
            uiBackground={900}
            uiSize={7}
            style={{
              pointerEvents: "auto"
            }}
          />
          {/* <Tooltip
            style={{
              height: 30,
              left: 0,
              position: "absolute",
              width: "100%"
            }}
          >
            <TooltipText position="top" uiSize="xl">
              Navigation is disabled. Please edit onPress in callbacks section
              below.
            </TooltipText>
          </Tooltip> */}
        </FormRow>
      </React.Fragment>
    );
  }
}
