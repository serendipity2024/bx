import * as React from "react";
import { FormRow, FormCol, Text, Scrollbars, TextArea } from "components-x";

export default class extends React.PureComponent<any, any> {
  render() {
    return (
      <FormRow key={"index.tsx"}>
        <FormCol first={true}>
          <Text uiSize="s">Callback Editor</Text>
        </FormCol>
        <FormCol uiSize={5}>
          <FormRow>
            <Scrollbars autoHide={true} autoHeight>
              <TextArea
                uiSize="xl"
                type="text"
                scroll={true}
                uiBackground={700}
                value="BuilderX Zindabad!"
              />
            </Scrollbars>
          </FormRow>
          <FormRow center noTopPadding>
            <Text style={{ color: "#bb1e1ecc" }}>
              "Error Message in Callback Editor"
            </Text>
          </FormRow>
        </FormCol>
      </FormRow>
    );
  }
}
