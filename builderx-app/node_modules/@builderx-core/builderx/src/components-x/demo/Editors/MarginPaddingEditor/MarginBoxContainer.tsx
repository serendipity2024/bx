import * as React from "react";
import MarginBox from "./MarginBox";

class MarginBoxContainer extends React.PureComponent<any, any> {
  render() {
    return <MarginBox title={this.props.title} />;
  }
}

export default MarginBoxContainer;
