import * as React from "react";
import { FormRow, ButtonGroup, Button, Icon, Text } from "components-x";

export default class ImageEditor extends React.PureComponent<any, any> {
  render() {
    return (
      <>
        <FormRow>
          <ButtonGroup>
            <Button flexible borderRadius border uiBackground={700}>
              <Text>Change Image</Text>
            </Button>
          </ButtonGroup>
        </FormRow>
        <FormRow>
          <Text>Resize Mode</Text>
        </FormRow>
        <FormRow>
          <ButtonGroup>
            <Button
              flexible
              borderRadius
              border
              uiBackground={700}
              icon={<Icon name="cover" size={16} />}
              active={true}
            />
            <Button
              flexible
              borderRadius
              border
              uiBackground={700}
              icon={<Icon name="stretch" size={16} />}
              active={false}
            />
            <Button
              flexible
              borderRadius
              border
              uiBackground={700}
              icon={<Icon name="contain" size={16} />}
              active={false}
            />
            <Button
              flexible
              borderRadius
              border
              uiBackground={700}
              icon={<Icon name="repeat" size={16} />}
              active={false}
            />
            <Button
              flexible
              borderRadius
              border
              uiBackground={700}
              icon={<Icon name="center" size={16} />}
              active={false}
            />
          </ButtonGroup>
        </FormRow>
      </>
    );
  }
}
