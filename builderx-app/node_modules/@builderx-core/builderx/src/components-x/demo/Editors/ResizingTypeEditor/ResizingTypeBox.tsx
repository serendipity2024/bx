import * as React from "react";
import {
  ResizingType,
  Row,
  Tooltip,
  ResizingTypeButton,
  FormRow,
  FormCol
} from "components-x";

export default class BorderWidthBox extends React.PureComponent<any, any> {
  render() {
    return (
      <ResizingType style={{ width: "210px" }}>
        <Row>
          <Tooltip>
            <ResizingTypeButton position="top" selected={top ? true : false} />
            {/* <TooltipText position="top" uiSize="xs">
              Fix top
            </TooltipText> */}
          </Tooltip>
        </Row>
        <FormRow center>
          <FormCol first>
            <Tooltip>
              <ResizingTypeButton position="left" selected={false} />
              {/* <TooltipText position="right" uiSize="xs">
                Fix left
              </TooltipText> */}
            </Tooltip>
          </FormCol>
          <FormCol first>
            <Tooltip>
              <ResizingTypeButton position="right" selected={false} />
              {/* <TooltipText position="right" uiSize="xs">
                Fix right
              </TooltipText> */}
            </Tooltip>
          </FormCol>
        </FormRow>
        <Row>
          <Tooltip>
            <ResizingTypeButton selected={false} />
            {/* <TooltipText position="bottom" uiSize="xs">
              Fix bottom
            </TooltipText> */}
          </Tooltip>
        </Row>
      </ResizingType>
    );
  }
}
