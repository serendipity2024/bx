import * as React from "react";
import {
  PropertyHeader,
  FormRow,
  FormCol,
  Slider,
  InputGroupInput,
  InputGroup,
  InputGroupLeft,
  InputGroupRight,
  Text
} from "components-x";
import { ColorPicker } from "components-x";

export class ShadowEditor extends React.PureComponent<any, any> {
  render() {
    return (
      <React.Fragment>
        <PropertyHeader
          uiSize="xs"
          open
          style={{ paddingTop: 5, paddingBottom: 0 }}
        >
          <Text uiSize="m">Shadow</Text>
        </PropertyHeader>

        <FormRow>
          <FormCol first uiSize={2}>
            {/* <ColorPicker value={"#000"} alignRight={true} /> */}
          </FormCol>
          <FormCol name="bx-shadow-colorPicker" uiSize={7}>
            <Slider min={0} max={100} name="bx-shadow-slider" value={1} />
          </FormCol>
          <FormCol uiSize={2}>
            <InputGroupInput
              undo={this.props.session.commandManager.undo}
              redo={this.props.session.commandManager.redo}
              type="number"
              min="0"
              max="100"
              name="bx-shadow-slider"
              value={1}
            />
          </FormCol>
        </FormRow>
        <FormRow>
          <FormCol first={true} uiSize={2}>
            <InputGroup>
              <InputGroupLeft>
                <Text uiSize="xs">X</Text>
              </InputGroupLeft>
              <InputGroupRight>
                <InputGroupInput
                  undo={this.props.session.commandManager.undo}
                  redo={this.props.session.commandManager.redo}
                  type="number"
                  name="bx-shadow-left"
                  value={0}
                />
              </InputGroupRight>
            </InputGroup>
          </FormCol>
          <FormCol uiSize={2}>
            <InputGroup>
              <InputGroupLeft>
                <Text uiSize="xs">Y</Text>
              </InputGroupLeft>
              <InputGroupRight>
                <InputGroupInput
                  undo={this.props.session.commandManager.undo}
                  redo={this.props.session.commandManager.redo}
                  type="number"
                  name="bx-shadow-top"
                  value={0}
                />
              </InputGroupRight>
            </InputGroup>
          </FormCol>
          <FormCol uiSize={3}>
            <InputGroup>
              <InputGroupLeft>
                <Text uiSize="xs">Blur</Text>
              </InputGroupLeft>
              <InputGroupRight>
                <InputGroupInput
                  undo={this.props.session.commandManager.undo}
                  redo={this.props.session.commandManager.redo}
                  type="number"
                  name="bx-shadow-radius"
                  value={0}
                />
              </InputGroupRight>
            </InputGroup>
          </FormCol>
        </FormRow>
      </React.Fragment>
    );
  }
}
