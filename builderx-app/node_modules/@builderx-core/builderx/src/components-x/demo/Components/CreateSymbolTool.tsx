import * as React from "react";
import { Button, Icon, MenuDropdown, OptionMenu, Option } from "../../lib";

export default class CreateSymbolTool extends React.PureComponent<any, any> {
  render() {
    return (
      <React.Fragment>
        <Button
          paddingLeft={3}
          transparent={true}
          icon={
            <Icon
              key="create-symbol-icon"
              name="symbol"
              size={16}
              marginRight
            />
          }
          caption="Create Symbol"
        />
        <MenuDropdown disabled={true} direction="left">
          <Option>
            <OptionMenu transparent disabled={true}>
              <Icon name="downarrow" size={12} />
            </OptionMenu>
          </Option>
        </MenuDropdown>
      </React.Fragment>
    );
  }
}
