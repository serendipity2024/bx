import * as React from "react";
import { FormRow, FormCol, Button, Text } from "components-x";

export default class Titlebar extends React.PureComponent<any, any> {
  render() {
    return (
      <FormRow>
        <FormCol first />
        <FormCol first>
          <Button
            transparent={true}
            flexible={true}
            textRight={true}
            paddingRight={10}
          >
            <Text uiSize="m">30 days left</Text>
          </Button>
        </FormCol>
      </FormRow>
    );
  }
}
