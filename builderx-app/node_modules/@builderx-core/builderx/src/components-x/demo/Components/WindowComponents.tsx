import * as React from "react";
import {
  Title,
  HorizontalDivider,
  Window,
  COLORS,
  WindowBody,
  WindowFooter,
  WrapperDiv,
  WindowHeader,
  WindowLeft,
  WindowRight,
  WindowTitle,
  WindowWrapper
} from "../../lib/core";

export default class WindowComponents extends React.Component<any, any> {
  constructor(props: any) {
    super(props);
  }

  render() {
    return (
      <>
        <Window uiBackground={800} uiSize="s">
          <WindowTitle uiSize="s">Window title with ui size m</WindowTitle>
          <WindowTitle marginTop={40} marginBottom={false}>
            Window Title with margin top as 40 px
          </WindowTitle>
          <WindowTitle marginTop={40} marginBottom={true}>
            Window Title with margin top as 40 px and margin Bottom true
          </WindowTitle>
          <WindowHeader uiBackground={200}>
            This is Window Header with uiBackground 200
          </WindowHeader>
          <WindowHeader uiSize="s">
            This is Window Header with uiSize s
          </WindowHeader>
          <WindowHeader uiSize="m">
            This is Window Header with uiSize m
          </WindowHeader>

          <WindowLeft uiBackground={200}>
            Left window with ui background 200
          </WindowLeft>
          <WindowLeft>Left Window without uiBackground</WindowLeft>
          <WindowRight>Window Right with no uiBackground</WindowRight>
          <WindowRight uiBackground={200}>
            Window Right with uiBackground 200
          </WindowRight>
          <WindowBody uiSize="m" uiBackground={800}>
            Window Body with uiSize m
          </WindowBody>
          <WindowFooter uiBackground={200} padder={true}>
            This is Window Footer with uiBackground and padder True
          </WindowFooter>
          <WindowFooter uiBackground={200} padder={false}>
            This is Window Footer with uiBackground and padder False
          </WindowFooter>

          {/* <WindowBody uiSize="m" uiBackground={800} padder={true}>
            Window Body with uiSize m,uiBackground 800,padder:true
          </WindowBody>
          <WindowFooter uiBackground={200} padder={false}>
            This is Window Footer with uiBackground and padder False
          </WindowFooter> */}
          {/* 
          <WindowBody uiSize="m" uiBackground={800} maxHeight={true}>
            Window Body with uiSize m,uiBackground 800,maxHeight:true
          </WindowBody>
          <WindowFooter uiBackground={200} padder={false}>
            This is Window Footer with uiBackground and padder False
          </WindowFooter> */}
        </Window>
      </>
    );
  }
}
