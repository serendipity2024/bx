import * as React from "react";
import {
  Section,
  SectionFooter,
  SectionBody,
  SectionHeader
} from "../../lib/core";

export default class SectionComponents extends React.Component<any, any> {
  constructor(props: any) {
    super(props);
  }

  render() {
    return (
      <>
        <Section>
          <SectionHeader uiSize="m" uiBackground={800}>
            This is section Header with ui size m and uiBackground 800
          </SectionHeader>
          <SectionHeader padder="l" uiBackground={200}>
            This is section Header with padder l
          </SectionHeader>
          <SectionBody padder="s">
            Section Body with padder s and no uiBackground
          </SectionBody>
          <SectionFooter padder="m" uiBackground={200}>
            This is section Footer with padder m and uiBackground 200
          </SectionFooter>
        </Section>
      </>
    );
  }
}
