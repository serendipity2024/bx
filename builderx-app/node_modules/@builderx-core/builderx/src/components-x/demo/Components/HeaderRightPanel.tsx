import * as React from "react";
import { Grid, Col } from "components-x";
import GroupToolbar from "./GroupToolbar";
import CreateSymbolTool from "./CreateSymbolTool";

export default class HeaderRightPanel extends React.PureComponent<any, any> {
  render() {
    return (
      <Grid>
        <Col>
          <GroupToolbar session={this.props.session} />
          <CreateSymbolTool session={this.props.session} />
        </Col>
      </Grid>
    );
  }
}
