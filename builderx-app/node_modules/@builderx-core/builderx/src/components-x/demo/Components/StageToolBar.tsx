import * as React from "react";
import { ButtonGroup, Button, Text } from "components-x";

export default class extends React.PureComponent<any, any> {
  render() {
    return (
      <ButtonGroup>
        <Button
          key="btn1"
          transparent={true}
          // onClick={() => {
          //   self.builderX.workspace.persist.editorState.setSelectedTool(
          //     "default"
          //   );
          // }}
          // tooltip={<TooltipText />}
        >
          <img key="image" alt="Logo" style={{ width: 20, height: 20 }} />
          <Text
            inline
            uiColor={300}
            uiSize="m"
            style={{
              paddingLeft: 6,
              fontWeight: 500,
              maxWidth: 250
            }}
          >
            {/* {project} */} Project-Name
          </Text>
        </Button>
        <Button key="btn2" transparent style={{ width: 40 }}>
          <Text key="edited" uiColor={400}>
            edited
          </Text>
        </Button>
      </ButtonGroup>
    );
  }
}
