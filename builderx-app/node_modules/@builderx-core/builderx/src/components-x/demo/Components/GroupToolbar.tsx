import * as React from "react";
import { ButtonGroup, Button, Icon } from "components-x";

export default class GroupToolbar extends React.PureComponent<any, any> {
  render() {
    return (
      <ButtonGroup>
        <Button
          paddingLeft={3}
          transparent={true}
          icon={
            <Icon key="group-icon" name="group" size={16} marginRight={true} />
          }
          caption="Group"
        />
        <Button
          paddingLeft={3}
          transparent={true}
          icon={
            <Icon
              key="ungroup-icon"
              name="ungroup"
              size={16}
              marginRight={true}
            />
          }
          caption="Ungroup"
        />
      </ButtonGroup>
    );
  }
}
