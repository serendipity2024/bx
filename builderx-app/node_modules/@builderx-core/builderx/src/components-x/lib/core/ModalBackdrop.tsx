import styled from "styled-components";
import COLORS from "./constants/COLORS";
import Background from "./types/BackgroundPropType";
import * as React from "react";
import createComponent from "../createComponent";
import * as classNames from "classnames";

interface PropType extends Background {}

export default createComponent(
  `
.modalBackdropX {
  position: absolute;
  top: 0;
  height: 100vh;
  left: 0;
  width: 100vw;
  z-index: 20000;
}
`,
  (props: PropType & React.HTMLProps<HTMLDivElement>) => {
    const { uiBackground, className, ...remainingProps } = props;
    const className2 = classNames({
      modalBackdropX: true
    });
    return (
      <div
        {...remainingProps}
        style={{
          ...remainingProps.style,
          background: uiBackground ? COLORS[uiBackground] : "transparent",
          opacity: uiBackground ? 0.6 : 1
        }}
        className={className2 + " " + className}
      />
    );
  }
);
