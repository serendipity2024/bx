const SPACER = 16;
const SIZES = [0, 0.25, 0.5, 0.75, 1.25, 1.75, 2.5, 5];

const SPACE_MAGNITUDES = SIZES.map(a => a * SPACER);

export const spacer = SPACER;
export const sizes = SIZES;
export const spaceMagnitudes = SPACE_MAGNITUDES;
