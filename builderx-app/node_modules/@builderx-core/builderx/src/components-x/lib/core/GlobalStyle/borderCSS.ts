import COLORS from "../constants/COLORS";

export default `
.bt {
    border-top: 1px solid ${COLORS[700]};
}
.bb {
    border-bottom: 1px solid ${COLORS[700]};
}

.bl {
    border-left: 1px solid ${COLORS[700]};
}

.br {
    border-right: 1px solid ${COLORS[700]};
}

`;
