import * as React from "react";
import SplitPane, { Props as SplitPaneProps } from "react-split-pane";
import "./index.scss";
interface PropType extends SplitPaneProps {
  key?: string;
  id?: string;
}
export default class extends React.Component<PropType, any> {
  render() {
    return (
      <SplitPane {...this.props} resizerStyle={{ backgroundColor: "none" }} />
    );
  }
}
