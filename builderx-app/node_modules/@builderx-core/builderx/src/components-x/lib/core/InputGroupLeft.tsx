import createComponent from "../createComponent";
import * as classNames from "classnames";
import * as React from "react";
// import COLORS from "./constants/COLORS";

interface PropType {
  width?: string;
  paddingLeft?: boolean;
}

export default createComponent(
  `
    .inputGroupLeftX {
      padding-left: 0;
      display: flex;

    }
    .inputGroupLeftX.paddingLeft{
      padding-left: 8px;
    }

`,
  (props: PropType & React.HTMLProps<HTMLDivElement>) => {
    const { paddingLeft, width, ...remainingProps } = props;

    const className = classNames({
      inputGroupLeftX: true,
      paddingLeft: paddingLeft ? true : false
    });

    return (
      <div
        {...remainingProps}
        style={{
          ...remainingProps.style,
          width: width ? width : "30%"
        }}
        className={
          className +
          (remainingProps.className ? " " + remainingProps.className : "")
        }
      />
    );
  }
);

// export default styled.div<PropType & React.HTMLProps<HTMLDivElement>>`
//   width: ${(props: PropType) => (props.width ? props.width : "30%")};
//   padding-left: ${(props: PropType) => (props.paddingLeft ? "8px" : "0px")};
//   display: flex;
// `;
