export default function createComponent<T>(css: string, component: T): T {
  if (typeof document !== "undefined") {
    var style = document.createElement("style");
    style.innerHTML = css;
    document.getElementsByTagName("head")[0].appendChild(style);
  }
  return component;
}
