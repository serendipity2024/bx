import { selectImageTool } from "./selectTool";
const fileName = "dashboard.jpg";
export default () => {
  cy.wait(20);

  cy.fixture(fileName, "base64").then(fileContent => {
    cy.get("input[type=file]")
      .first()
      .upload(
        { fileContent, fileName: fileName, mimeType: "image/png" },
        { subjectType: "input" }
      );
  });
};
