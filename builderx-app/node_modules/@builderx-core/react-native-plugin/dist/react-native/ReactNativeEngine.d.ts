import Engine from '../types/Engine';
import { Config } from './types';
export default class ReactNativeEngine implements Engine {
    static engineName: string;
    getFileCode(fileJSON: any, config: Config): any;
    getAppFiles(files: {
        artboards: string[];
        assets: string[];
        missingAssets: any[];
    }): {
        path: string;
        content: any;
    }[];
    getIndexWithFont(assets: string[]): {
        path: string;
        content: any;
    }[];
}
