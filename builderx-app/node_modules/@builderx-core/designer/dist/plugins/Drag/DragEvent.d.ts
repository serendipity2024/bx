import { Layer } from '../../models/Layer';
import DesignerMouseEvent from '../../events/MouseEvent';
export default class DragEvent extends DesignerMouseEvent {
    pageDx: number;
    pageDy: number;
    worldDx: number;
    worldDy: number;
    viewportDx: number;
    viewportDy: number;
    constructor(name: string, target: Layer, originalEvent: any, payload: any, currentTarget?: Layer);
}
