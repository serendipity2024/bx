import { Layer, MouseEvent } from '../../..';
import { IPosition, IDimensionWithRotation } from 'interfaces';
export declare class ResizerRendererDragEvent extends MouseEvent {
    targetHandle: string;
    startValues: IDimensionWithRotation;
    session: {
        start: IPosition;
        current: any;
        end: IPosition;
    };
    constructor(name: string, targetHandle: string, startValues: IDimensionWithRotation, session: {
        start: IPosition;
        current: IPosition;
        end: IPosition;
    }, originalEvent: any, target: Layer, currentTarget?: Layer);
}
