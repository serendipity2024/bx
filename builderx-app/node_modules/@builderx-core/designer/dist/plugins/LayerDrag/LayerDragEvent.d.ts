import { Layer } from '../../models/Layer';
import DesignerMouseEvent from '../../events/MouseEvent';
export default class LayerDragEvent extends DesignerMouseEvent {
    layout: any;
    meta: any;
    snapPoints: any;
    constructor(name: string, target: Layer, originalEvent: any, payload: {
        layerWorldX: number;
        layerWorldY: number;
        meta?: any;
        snapPoints?: any;
    }, currentTarget?: Layer);
}
