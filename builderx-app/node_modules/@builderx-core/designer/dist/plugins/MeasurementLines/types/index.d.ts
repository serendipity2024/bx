import { Canvas } from '../../../';
export interface Box {
    height: number;
    width: number;
    x: number;
    y: number;
}
export interface PropTypes {
    box: Box;
    helperBox: Box;
    canvas: Canvas;
}
