import { Event, Layer } from './../../..';
export default class MeasurementLinesEvent extends Event {
    payload: Array<Layer>;
    constructor(name: string, target: Layer, payload: Array<Layer>, currentTarget?: Layer);
}
