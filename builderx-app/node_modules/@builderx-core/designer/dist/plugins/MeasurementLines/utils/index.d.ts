import { Box } from '../types';
export declare function isContained(axis: string, box: Box, helperBox: Box): boolean;
export declare function isAlignedonAxis(axis: string, box: any, helperBox: any): boolean;
export declare function getDirection(axis: string, box: Box, helperBox: Box): string;
