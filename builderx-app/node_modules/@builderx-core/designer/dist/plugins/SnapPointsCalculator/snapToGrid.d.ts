import { IContext } from 'interfaces';
import Config from './interfaces/Config';
export default function snapToGrid(config: Config, context: IContext, startX: number, totalDeltaX: number, startY: number, totalDeltaY: number, stretchX: number, stretchY: number, handle?: any): {
    x: any;
    y: any;
} | undefined;
