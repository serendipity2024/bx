import { Event, Layer } from '../..';
export declare class FontResizerEvent extends Event {
    fontSize: number;
    y: number;
    x: number;
    constructor(name: string, target: Layer, payload: {
        fontSize?: number;
        x?: number;
        y?: number;
    }, currentTarget?: Layer);
}
