import { Layer, MouseEvent } from '../../..';
import { IPosition, IDimensionWithRotation } from 'interfaces';
export declare class LineDrawerDragEvent extends MouseEvent {
    startValues: IDimensionWithRotation;
    session: {
        start: IPosition;
        current: IPosition;
        end: IPosition;
    };
    axis?: 'x' | 'y';
    constructor(name: string, startValues: IDimensionWithRotation, session: {
        start: IPosition;
        current: IPosition;
        end: IPosition;
    }, originalEvent: any, target: Layer, currentTarget?: Layer, axis?: 'x' | 'y');
}
