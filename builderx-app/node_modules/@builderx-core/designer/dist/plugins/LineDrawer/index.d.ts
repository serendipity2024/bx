import { IPlugin, IContext } from 'interfaces';
import { Canvas } from 'models/Canvas';
export default class LineDrawer implements IPlugin {
    name: string;
    canvas: Canvas;
    namespace: string;
    ghostImageDOM?: any;
    dispose: any;
    handleGhostImage(e: any): void;
    disposeGhostImage(): void;
    drawLine(context: IContext, axis: 'x' | 'y', axisCoordinate: number, className: string | null, startCoordinate?: number, endCoordinate?: number, draggable?: boolean, label?: string, style?: any): HTMLDivElement;
    setLabelBoxStyle(labelBox: any, height: any, width: any, axis: any, labelSpacing: any, pointOnAxis: any, text: any, fontSize: any): void;
    createLabelBox(label: string | undefined, axis: 'x' | 'y', coordinate: number, pointOnAxis: number, boxHeight: number, boxWidth: number, labelSpacing: number, fontSize: number): HTMLDivElement | undefined;
    init(canvas: Canvas): void;
    activate(): void;
    deactivate(): void;
}
