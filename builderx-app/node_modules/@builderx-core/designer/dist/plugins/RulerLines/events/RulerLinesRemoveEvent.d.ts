import { Layer, Event } from '../../..';
export default class RulerLinesRemoveEvent extends Event {
    index: number;
    constructor(name: string, target: Layer, index: number, currentTarget?: Layer);
}
