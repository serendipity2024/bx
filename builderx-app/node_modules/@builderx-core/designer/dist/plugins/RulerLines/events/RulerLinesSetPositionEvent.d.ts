import { Layer, Event } from '../../..';
export default class RulerLinesSetPositionEvent extends Event {
    axis: 'x' | 'y';
    axisCoordinate: number;
    index?: number;
    constructor(name: string, target: Layer, axis: 'x' | 'y', axisCoordinate: number, index?: number, currentTarget?: Layer);
}
