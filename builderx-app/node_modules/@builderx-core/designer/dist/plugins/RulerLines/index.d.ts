import { Canvas, IPlugin, IContext } from '../..';
import IRulerLine from './interfaces/IRulerLine';
export default class RulerLinesPlugin implements IPlugin {
    name: string;
    canvas: Canvas;
    lines: Array<IRulerLine>;
    init(canvas: Canvas): void;
    addLine: (context: IContext, axis: 'x' | 'y', axisCoordinate: number, noEmit?: boolean) => number;
    removeLine: (context: IContext, index: number) => void;
    setPosition: (context: IContext, axisCoordinate: number, index?: number | undefined, dragEnd?: boolean | undefined) => void;
    removeAll: (context: IContext) => void;
    addAll: (context: IContext, data: [{
        axis: 'x' | 'y';
        axisCoordinate: number;
    }], emit?: boolean) => void;
    show: (context: IContext) => void;
    hide: (context: IContext) => void;
    getLineCoordinates: (context: IContext) => {
        x: Array<number>;
        y: Array<number>;
    };
    activate(): void;
    deactivate(): void;
}
