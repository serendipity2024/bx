import { Layer } from '../models/Layer';
import { Event } from './index';
export default class ArtboardEvent extends Event {
    artboard: Layer;
    constructor(name: string, target: Layer, artboard: Layer, currentTarget?: Layer);
}
