import { Layer } from '../models/Layer';
import { Event } from './index';
export default class SelectLayers extends Event {
    layers: Array<Layer>;
    constructor(name: string, target: Layer, layers: Array<Layer>, currentTarget?: Layer);
}
