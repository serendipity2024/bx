import { Layer } from '../models/Layer';
import { Event } from './index';
export default class SetScrollEvent extends Event {
    scrollX: number;
    scrollY: number;
    constructor(name: string, target: Layer, originalEvent: any, currentTarget?: Layer);
}
