import { Layer } from '../models/Layer';
import { Event } from './index';
export default class SetZoomEvent extends Event {
    zoom: number;
    constructor(name: string, target: Layer, zoom: number, currentTarget?: Layer);
}
