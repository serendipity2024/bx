import { Layer } from '../models/Layer';
import { Event } from './index';
export default class HoverEvent extends Event {
    payload: Array<Layer>;
    constructor(name: string, target: Layer, payload: Array<Layer>, currentTarget?: Layer);
}
