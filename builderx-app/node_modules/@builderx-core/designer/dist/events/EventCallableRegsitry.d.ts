import { IEventCallable } from './IEventCallable';
import { Event } from './index';
export declare class EventCallableRegsitry implements IEventCallable {
    registry: Map<string, Array<(event: Event) => {}>>;
    constructor();
    addEventListener(name: string, callback: any): void;
    removeEventListener(name: string, callback: any): void;
    dispatchEvent(name: string, event: Event): void;
}
