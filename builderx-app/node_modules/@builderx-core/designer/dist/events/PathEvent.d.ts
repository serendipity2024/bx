import { Layer } from '../models/Layer';
import { Event } from './index';
export default class PathEvent extends Event {
    payload: any;
    constructor(name: string, target: Layer, payload?: any, currentTarget?: Layer);
}
